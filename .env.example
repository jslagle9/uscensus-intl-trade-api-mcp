# Get a free key at https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=your_40_character_key_here

# Optional: "stdio" (default, for local MCP clients) or "http" (remote server)
TRANSPORT=stdio

# Only used when TRANSPORT=http
PORT=3000
